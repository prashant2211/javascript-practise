// picking element by using for loop ::  this code is no completed

var element_number = prompt("Enter the number word");
let element = 0;
for(let i = 0; i = element; i++)
{
    element = prompt("Enter the element: ");

}
console.log(element);